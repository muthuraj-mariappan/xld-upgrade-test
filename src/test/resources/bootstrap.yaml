spring:
  application:
    name: "deploy"
  profiles:
    active: server
  cloud:
    config:
      uri: http://localhost:4519/centralConfiguration
      fail-fast: true
      username: _xl-deploy-config-admin_
      password: admin